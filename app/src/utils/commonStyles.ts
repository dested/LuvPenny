import {StyleSheet} from 'react-native'

export let CommonStyles = StyleSheet.create({
    centerContent: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
});
export let baseTextColor = {
    color:'#223843'
};
export let pageBackgroundColor={
    backgroundColor: '#FFFFFF',
};
export let thinFont = {
    // fontFamily:'sans-serif-thin'
};
