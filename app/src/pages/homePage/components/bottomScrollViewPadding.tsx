import React from "react";
import {View} from "react-native";

export let BottomScrollViewPadding: React.SFC<{}> = (props) => {
    return (
        <View style={{height: 50}}>
        </View>
    )
};
