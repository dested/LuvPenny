import {StyleSheet} from 'react-native';

export let styles = StyleSheet.create({
    bottomLeftIcon: {
        position: 'absolute',
        bottom: 10
    },
    bottomRightIcon: {
        position: 'absolute',
        bottom: 10
    }
});
